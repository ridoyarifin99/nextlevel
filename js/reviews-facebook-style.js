"use strict";
(() => {
  const path=window.location.pathname;
  if(!/\/details\.html$/i.test(path)&&! /\/product\//i.test(path))return;
  const db=()=>window.supabaseClient;
  let busy=false,decorating=false,observerTimer=0;
  const getUser=async()=>{try{return(await db().auth.getUser()).data?.user||null}catch(_){return null}};
  function injectStyles(){if(document.getElementById("nls-facebook-review-styles"))return;const s=document.createElement("style");s.id="nls-facebook-review-styles";s.textContent=`.nls-fb-action{border:0;background:transparent;padding:5px 7px;border-radius:8px;color:#64748b;font-size:12px;font-weight:800;cursor:pointer;transition:background .2s,color .2s,transform .2s;display:inline-flex;align-items:center;gap:5px}.nls-fb-action:hover{background:#f3f4f6;color:#4c1d95}.nls-fb-action.liked{color:#6d28d9;background:#f3e8ff}.nls-fb-action .count{font-size:11px;color:#94a3b8}.nls-fb-reply-actions{display:flex;gap:3px;margin-top:4px}.nls-existing-reply{transition:background .2s}.nls-existing-reply:hover{background:#f1f5f9}.nls-fb-reaction-pop{position:fixed;z-index:10080;padding:8px 11px;border-radius:999px;background:#111827;color:#fff;font-size:11px;box-shadow:0 10px 30px rgba(0,0,0,.2);animation:nlsFbPop .25s ease both}@keyframes nlsFbPop{from{opacity:0;transform:scale(.8) translateY(4px)}to{opacity:1;transform:none}}`;document.head.appendChild(s)}
  async function reactionState(reviewIds,replyIds,userId){const out={reviews:{},replies:{}};if(reviewIds.length){const {data}=await db().from("review_reactions").select("review_id,user_id").in("review_id",reviewIds);(data||[]).forEach(x=>{const a=out.reviews[x.review_id] ||= {count:0,liked:false};a.count++;if(x.user_id===userId)a.liked=true})}if(replyIds.length){const {data}=await db().from("review_reactions").select("reply_id,user_id").in("reply_id",replyIds);(data||[]).forEach(x=>{const a=out.replies[x.reply_id] ||= {count:0,liked:false};a.count++;if(x.user_id===userId)a.liked=true})}return out}
  function syncButton(btn,state){const s=state||{count:0,liked:false};btn.classList.toggle("liked",!!s.liked);btn.setAttribute("aria-label",s.liked?"Unlike":"Like");btn.innerHTML=`<i class="${s.liked?"fas":"far"} fa-thumbs-up"></i><span>Like</span>${s.count?`<span class="count">${s.count}</span>`:""}`}
  function newButton(type,id,state){const b=document.createElement("button");b.type="button";b.className="nls-fb-action";b.dataset.fbLike="1";b.dataset.fbType=type;b.dataset.fbId=id;syncButton(b,state);return b}
  function reactionToast(x){const el=document.createElement("div");el.className="nls-fb-reaction-pop";el.textContent=x;document.body.appendChild(el);const r=el.getBoundingClientRect();el.style.left=`${Math.max(8,Math.min(innerWidth-r.width-8,innerWidth/2-r.width/2))}px`;el.style.top=`${Math.max(12,innerHeight/2-70)}px`;setTimeout(()=>el.remove(),900)}
  async function toggle(type,id,buttonEl){const user=await getUser();if(!user){reactionToast("Please log in to like this comment");return}if(busy)return;busy=true;buttonEl.disabled=true;try{const column=type==="review"?"review_id":"reply_id";const {data:existing,error:qerr}=await db().from("review_reactions").select("id").eq(column,id).eq("user_id",user.id).eq("reaction_type","like").maybeSingle();if(qerr)throw qerr;if(existing){const {error}=await db().from("review_reactions").delete().eq("id",existing.id);if(error)throw error}else{const payload={user_id:user.id,reaction_type:"like"};payload[column]=id;const {error}=await db().from("review_reactions").insert(payload);if(error)throw error}await decorate()}catch(e){console.error(e);reactionToast(e?.message||"Unable to update reaction")}finally{busy=false;buttonEl.disabled=false}}
  async function decorate(){if(decorating)return;const cards=[...document.querySelectorAll("#tabContent .nls-review-card[data-review-id]")];if(!cards.length)return;decorating=true;try{const reviewIds=cards.map(x=>x.dataset.reviewId),replyEls=[...document.querySelectorAll("#tabContent .nls-existing-reply")],replyIds=[];if(replyEls.length){const {data}=await db().from("review_replies").select("id,review_id,created_at").in("review_id",reviewIds).order("created_at",{ascending:true});const byReview={};(data||[]).forEach(r=>(byReview[r.review_id] ||= []).push(r.id));cards.forEach(card=>{const ids=byReview[card.dataset.reviewId]||[],els=[...card.querySelectorAll(".nls-existing-reply")];els.forEach((el,i)=>{if(ids[i])el.dataset.fbReplyId=ids[i];if(el.dataset.fbReplyId)replyIds.push(el.dataset.fbReplyId)})})}
      const user=await getUser(),state=await reactionState(reviewIds,replyIds,user?.id);
      cards.forEach(card=>{const id=card.dataset.reviewId,actions=card.querySelector(".nls-review-actions");if(!actions)return;let like=actions.querySelector("[data-fb-like]");if(!like){like=newButton("review",id,state.reviews[id]);actions.insertBefore(like,actions.firstChild)}else syncButton(like,state.reviews[id]);like.dataset.fbId=id;like.dataset.fbType="review";if(!like.dataset.bound){like.dataset.bound="1";like.addEventListener("click",()=>toggle("review",id,like))}});
      replyEls.forEach(el=>{const id=el.dataset.fbReplyId;if(!id)return;let actions=el.querySelector(".nls-fb-reply-actions");if(!actions){actions=document.createElement("div");actions.className="nls-fb-reply-actions";el.appendChild(actions)}let like=actions.querySelector("[data-fb-like]");if(!like){like=newButton("reply",id,state.replies[id]);actions.appendChild(like)}else syncButton(like,state.replies[id]);if(!like.dataset.bound){like.dataset.bound="1";like.addEventListener("click",()=>toggle("reply",id,like))}});
    }finally{decorating=false}}
  function schedule(){clearTimeout(observerTimer);observerTimer=setTimeout(()=>{if(document.querySelector("#tabContent .nls-review-card"))decorate()},100)}
  function install(){injectStyles();if(typeof window.switchTab!=="function"){setTimeout(install,80);return}if(window.switchTab.__nlsFacebook)return;const original=window.switchTab;const wrapped=function(tab){const result=original.apply(this,arguments);if(tab==="reviews")setTimeout(decorate,180);return result};wrapped.__nlsFacebook=true;window.switchTab=wrapped;const box=document.getElementById("tabContent");if(box)new MutationObserver(schedule).observe(box,{childList:true,subtree:true});setTimeout(schedule,500)}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",install,{once:true});else install();
})();
