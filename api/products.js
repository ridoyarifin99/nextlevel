"use strict";
const SUPABASE_URL=process.env.SUPABASE_URL||"https://zrptkmjdltqdjzrpogyo.supabase.co";
const SUPABASE_KEY=process.env.SUPABASE_ANON_KEY||process.env.SUPABASE_PUBLISHABLE_KEY||"sb_publishable_KcWSkkO1L4z0U6UUfZijyw_KIJ_d5m7";
async function query(table,select){const u=`${SUPABASE_URL}/rest/v1/${table}?select=${encodeURIComponent(select)}`;const r=await fetch(u,{headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`}});if(!r.ok)throw Error(`${table} request failed (${r.status})`);return r.json()}
module.exports=async function(req,res){try{if(req.method!=="GET"&&req.method!=="HEAD"){res.statusCode=405;res.setHeader("Allow","GET, HEAD");return res.end("Method Not Allowed")}const products=await query("products","id,category_id,name,slug,description,image_url,icon,brand_color,currency,price,old_price,is_available,display_order,is_featured,badge,features,faq,keywords,seo_title,seo_description,seo_canonical,services,extra_data,is_archived,product_categories(id,name,slug),product_plans(id,name,duration,price,old_price,currency,is_available,display_order,features,extra_data)");const clean=(products||[]).filter(p=>p.is_available&&!p.is_archived).map(p=>({...p,product_plans:(p.product_plans||[]).filter(x=>x.is_available!==false).sort((a,b)=>(a.display_order||0)-(b.display_order||0))})).sort((a,b)=>(a.display_order||0)-(b.display_order||0)).map(p=>({...p,url:`/product/${p.slug}`,canonicalUrl:p.seo_canonical||`https://www.nextlevelsubs.com/product/${p.slug}`,categories:p.product_categories?[p.product_categories.slug]:[]}));res.statusCode=200;res.setHeader("Content-Type","application/json; charset=utf-8");res.setHeader("Cache-Control","public, max-age=60, s-maxage=300, stale-while-revalidate=3600");if(req.method==="HEAD")return res.end();res.end(JSON.stringify({products:clean}))}catch(e){console.error(e);res.statusCode=500;res.setHeader("Content-Type","application/json");res.end(JSON.stringify({error:"Unable to load product catalog"}))}};
