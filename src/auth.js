"use strict";

// Compatibility shim for legacy pages that still reference /src/auth.js.
// The active Supabase authentication implementation is /js/auth.js,
// which is loaded after the Supabase client has been initialized.
